---
# Allowed version bumps: patch, minor, major
"@jahia/moonstone": minor
---

`DateTimeInput` (beta): accept `Date` and `Temporal.Instant` values in `type="zonedDateTime"`, add `fallbackTimeZone` to anchor zone-less values, and forward data attributes through `timeInputProps`

A generic item of a list.

## Specifications:

It should be clickable, selectable, focusable
Can start with an icon, end by an icon
The width is fluid
The label should be truncated if it's longer than the container

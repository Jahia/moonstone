# Moonstone — Copilot instructions

The agent baseline for this repo is **`AGENTS.md`** at the repo root. Read it first: it holds
the rules every task follows and the map of where each standard lives (under `docs/`).

Per-file triggers live in `.github/instructions/`.

---
description: "Use when creating, updating, or auditing the documentation of a component (its .md prose and .mdx Storybook Docs page under src/components/)."
applyTo: "src/components/**/*.{md,mdx}"
---
# Component documentation

The rules for writing component docs live in **`docs/contributing/component-docs.md`** — the
single source. Follow it. Prefer the **`component-docs` agent** for any non-trivial write or audit.

See `AGENTS.md` (repo root) for the general agent rules.

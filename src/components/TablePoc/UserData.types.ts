export type UserDataTypes = {
  firstName: string;
  lastName: string;
  age: number;
  status: 'Accept' | 'In progress' | 'Refuse';
  progress: number;
  date: Date;
};

export type ProductDataTypes = {
  productName: string;
  category: string;
  id: number;}

---
"@jahia/moonstone": minor
---

Add `minDateTime` / `maxDateTime` to `DateTimeInput` (`dateTime` and `zonedDateTime`): inclusive minute-precision bounds that disable the days outside them in the calendar and clamp a time that would overshoot on the boundary day
